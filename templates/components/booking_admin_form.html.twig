<div {{ attributes }}>
{{ form_start(form, {'attr': {'id': 'booking'}}) }}
    {{ form_row(form.user) }}
    {{ form_widget(form.beginAt) }}
    {{ form_widget(form.endAt) }}
    {{ form_row(form.title) }}
    {{ form_row(form.description) }}
    <button id="create-booking" class="btn">{{ button_label|default('Enregistrer') }}</button>
{{ form_end(form) }}
</div>